//Have the function CheckNums(num1,num2) take both parameters being passed
//Return the string true if num2 is greater than num1, otherwise return the string false. 
//If the parameter values are equal to each other then return the string -1. 

function CheckNums(num1,num2) { //Simple if else function
  if(num1===num2){return -1;}
    else if(num1<num2){return true;}
    else{return false;}
    
}
